{
  "name": "minkert",
  "private": true,
  "description": "Запуск фронта и API одной командой (чтобы не было 502 из-за выключенного бэкенда)",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx --yes concurrently -n api,web -c blue,magenta \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run setup && npm run dev"
  }
}
